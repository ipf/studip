<Directory "/srv/www/htdocs/studip">
    # für rewrite wird die Option FollowSymLinks oder SymLinksIfOwnerMatch benötigt ...
    #Options SymLinksIfOwnerMatch
    #RewriteEngine on
    #RewriteRule ^download/(normal|force_download|zip)/([0-46])/([^/]+)/(.+)$ sendfile.php?$1=1&type=$2&file_id=$3&file_name=$4 [L]
    #RewriteRule ^download/(normal|force_download|zip)/5/([^/]+)/([^/]+)/(.+)$ sendfile.php?$1=1&type=5&range_id=$2&list_id=$3&file_name=$4 [L]
    #bzw. bei Verwendung von Alias:
    #RewriteEngine on
    #RewriteBase /srv/studip/htdocs
    #RewriteRule ^download/(normal|force_download|zip)/([0-46])/([0-9a-f]+)/(.+)$ /studip/sendfile.php?$1=1&type=$2&file_id=$3&file_name=$4 [L]
    #RewriteRule ^download/(normal|force_download|zip)/5/([^/]+)/([^/]+)/(.+)$ /studip/sendfile.php?$1=1&type=5&range_id=$2&list_id=$3&file_name=$4 [L]
    	
    php_value auto_prepend_file prepend4.php
    php_value include_path .:/srv/www/studip-phplib
    php_value upload_max_filesize 7M
    php_value post_max_size 8M
    php_value memory_limit 20M
    php_value max_execution_time 300
    php_flag short_open_tag On
    php_flag register_globals On
    php_flag session.use_trans_sid Off
    php_admin_flag allow_url_fopen On
</Directory>